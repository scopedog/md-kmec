# Out-of-tree build for the md-kmec redesign.
#
# Builds km/raid_km.ko — a fork of raid5.c registered as a single
# md personality at level 71.  Behavior is identical to stock raid6
# at this point in history; the k+m generalization (ISA-L
# encode/decode, m-way recovery) is staged in over subsequent
# commits.

obj-m += km/
